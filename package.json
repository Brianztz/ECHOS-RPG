{
  "name": "echos-rpg",
  "version": "2.0.0",
  "private": true,
  "description": "ECHOS RPG — ficha e Escudo do Mestre em tempo real, preparados para Cloudflare Workers + Durable Objects.",
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "start:node": "node server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
